# ─── Heredar todas las deps de producción ───────────────
-r requirements.txt

# ─── Testing ────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.1.0
respx==0.22.0            # mock de httpx para unit tests sin I/O real
pytest-mock==3.14.0

# ─── Code quality ───────────────────────────────────────
ruff==0.9.10             # linter + formatter (reemplaza flake8 + black)
mypy==1.15.0
